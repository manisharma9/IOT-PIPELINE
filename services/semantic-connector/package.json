{
  "name": "adflex-semantic-connector",
  "version": "0.2.0",
  "private": true,
  "description": "Local SLM-primary semantic connector with deterministic SAREF4ENER validation and fallback.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "pg": "^8.12.0"
  }
}
