# WAF Module Placeholder

Future purpose:

- Rate-based rules.
- IP allow/block sets.
- Managed SQL injection and XSS rule groups.
- Custom inspection rules for path traversal and command injection patterns.

Local mapping:

- Security gateway rate limiting, IP filtering, and DPI-style inspection.

Credentials needed later:

- AWS deployment role only. Rule configuration should not contain secrets.
