# Secrets Module Placeholder

Future purpose:

- Store API keys, connector credentials, JWT settings, and service credentials in AWS Secrets Manager or SSM Parameter Store.

Local mapping:

- `.env.example` documents local development variables only.

Credentials needed later:

- Real AWS secret names and rotation policy
- Real ENERSHARE credentials
- Real Shelly credentials
- Real Enode/Easee credentials

Never commit real secret values.
