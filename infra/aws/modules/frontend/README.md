# Frontend Module Placeholder

Future purpose:

- Host a frontend through CloudFront/S3 or Amplify.
- Ensure the frontend calls API Gateway only.
- Configure CORS against the API Gateway domain.

Local mapping:

- Local frontend calls should use `http://localhost:3010` during development.

Credentials needed later:

- Domain and certificate details
- CI/CD deployment credentials
